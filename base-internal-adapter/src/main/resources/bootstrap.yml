spring:
  application:
    name: base-internal-adapter
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        prefer-ip-address: true

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: ALWAYS